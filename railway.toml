# Railway service root should be the monorepo root (repo root).
# The Dockerfile copies apps/api/, packages/, etc. from that context.
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"

# Healthcheck disabled — verify manually after deploy: GET /health and GET /health/ready
[deploy]
healthcheckPath = ""
